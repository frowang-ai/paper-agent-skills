# Paper Library skill 配置示例
# 复制为同目录下的 .env 并填入真实值：cp .env.example .env

# 必填：API Key（在网站个人中心生成，pk_ 开头）
PAPER_API_KEY=pk_your_api_key_here

# 可选：API 地址（默认如下，通常无需改）
PAPER_API_BASE_URL=https://frowang.com/paper-api/api/v1
